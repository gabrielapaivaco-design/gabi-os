NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key
ANTHROPIC_API_KEY=sua-chave-anthropic

# Quem pode CRIAR conta (e-mails separados por virgula).
# Entrar com conta existente nunca depende disso.
#
# Em producao, sem esta variavel o cadastro fica FECHADO — de proposito. Os
# dados de cada conta sao isolados pelo RLS, mas a chave da Anthropic e uma so:
# a geracao de qualquer pessoa sai do credito de quem publicou o sistema.
SIGNUP_ALLOWED_EMAILS=voce@exemplo.com
